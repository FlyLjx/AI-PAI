name: ai-pai

services:
  postgres:
    image: postgres:16
    container_name: ai-pai-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-ai_pai}
      POSTGRES_USER: ${DB_USER:-ai_pai}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-ai_pai_change_me}
      TZ: ${TZ:-Asia/Shanghai}
    ports:
      - "${POSTGRES_PUBLIC_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-ai_pai} -d ${DB_NAME:-ai_pai}"]
      interval: 5s
      timeout: 5s
      retries: 20

  api:
    build:
      context: ./go-server
      dockerfile: Dockerfile
      args:
        GOPROXY: ${GOPROXY:-https://proxy.golang.org,direct}
    image: ${API_IMAGE:-ghcr.io/flyljx/ai-pai-api:latest}
    container_name: ai-pai-api
    restart: unless-stopped
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      DB_DRIVER: ${DB_DRIVER:-postgres}
      DB_HOST: ${DB_HOST:-postgres}
      DB_PORT: ${DB_PORT:-5432}
      DB_USER: ${DB_USER:-ai_pai}
      DB_PASSWORD: ${DB_PASSWORD:-ai_pai_change_me}
      DB_NAME: ${DB_NAME:-ai_pai}
      DB_SSLMODE: ${DB_SSLMODE:-disable}
      SERVE_STATIC: "false"
      LOG_DIR: /app/logs
      SYSTEM_UPDATE_DIR: /app/update
      GITHUB_UPDATE_REPOSITORY: ${GITHUB_UPDATE_REPOSITORY:-FlyLjx/AI-PAI}
      GITHUB_UPDATE_WORKFLOW: ${GITHUB_UPDATE_WORKFLOW:-build.yml}
      APP_PUBLIC_ORIGIN: ${APP_PUBLIC_ORIGIN:-http://127.0.0.1:6985}
      PORT: 3001
    depends_on:
      postgres:
        condition: service_healthy
    expose:
      - "3001"
    volumes:
      - ./logs:/app/logs
      - ./update:/app/update

  ai-pai:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${WEB_IMAGE:-ghcr.io/flyljx/ai-pai-web:latest}
    container_name: ai-pai
    restart: unless-stopped
    environment:
      GO_BACKEND_URL: http://api:3001
      APP_PUBLIC_ORIGIN: ${APP_PUBLIC_ORIGIN:-http://127.0.0.1:6985}
      AUTH_ACTION_URLS_IN_RESPONSE: ${AUTH_ACTION_URLS_IN_RESPONSE:-false}
      ADMIN_INTERNAL_URL: http://admin:3002
      PORT: 3000
    depends_on:
      api:
        condition: service_started
      admin:
        condition: service_healthy
    ports:
      - "${APP_PUBLIC_PORT:-6985}:3000"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/healthz').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  admin:
    build:
      context: .
      dockerfile: Dockerfile.admin
    image: ${ADMIN_IMAGE:-ghcr.io/flyljx/ai-pai-admin:latest}
    container_name: ai-pai-admin
    restart: unless-stopped
    environment:
      GO_BACKEND_URL: http://api:3001
      ADMIN_COOKIE_SECURE: ${ADMIN_COOKIE_SECURE:-false}
      APP_PUBLIC_ORIGIN: ${APP_PUBLIC_ORIGIN:-http://127.0.0.1:6985}
      PORT: 3002
    depends_on:
      - api
    expose:
      - "3002"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3002/sys-admins/healthz').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

volumes:
  postgres_data:
